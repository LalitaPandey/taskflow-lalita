# Backend — runs json-server + json-server-auth on port 4000
FROM node:20-alpine

WORKDIR /app

COPY package*.json ./
RUN npm install

COPY server.js db.json ./

EXPOSE 4000

CMD ["node", "server.js"]
